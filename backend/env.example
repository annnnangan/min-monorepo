ENV=development
PORT=10000
# Comma-separated list of allowed CORS origins (works in both .env and actual environment variables)
CORS_ORIGINS=http://localhost:5173,https://your-frontend-url.onrender.com
# Optional: regex pattern for allowed origins (e.g., ^https://.*\\.example\\.com$). Leave blank to disable.
CORS_ORIGIN_REGEX=